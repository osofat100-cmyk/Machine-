# Flat dependency list for running without installing the package:
#   pip install -r requirements.txt && python run_simulation.py
# Preferred: pip install -e ".[test,tools]"  (same packages, plus the `slab-sim` command).
# Keep in sync with pyproject.toml ([project] dependencies + the test/tools extras); tests/test_packaging.py checks it.
# runtime (pyproject: dependencies)
numpy>=1.26
scipy>=1.11
h5py>=3.9
mpmath>=1.3
# symbolic re-derivation tools/derive_ef_curvature.py (pyproject: extra "tools")
sympy>=1.12
# tests (pyproject: extra "test")
pytest>=7
