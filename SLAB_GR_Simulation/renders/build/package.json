{
  "name": "slab-viewer-build",
  "version": "0.1.0",
  "private": true,
  "description": "Build and test toolchain of the SLAB_GR_Simulation static three.js viewer (renders/viewer.html)",
  "scripts": {
    "build": "NODE_PATH=\"$PWD/node_modules\" esbuild ../src/main.js --bundle --format=iife --target=es2020 --outfile=../viewer.bundle.js --log-level=warning",
    "test": "npm run --silent test:physics && npm run --silent test:viewer",
    "test:physics": "for f in ../../tests/viewer/test_*.mjs; do echo \"== node $f\"; node \"$f\" || exit 1; done",
    "test:viewer": "node ../../tests/viewer/check_viewer.mjs && node ../../tests/viewer/causal_check.mjs",
    "browser:install": "npx --yes playwright@$(node -p \"require('playwright-core/package.json').version\") install --with-deps chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "esbuild": "0.24.0",
    "three": "0.170.0"
  },
  "devDependencies": {
    "playwright-core": "1.49.1"
  }
}
