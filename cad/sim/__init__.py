"""Driving the arm through a motion program, and drawing the result.

`kinematics` solves poses, `program` turns a cell program into one solved
state per frame, `scene` turns the CAD assembly into triangles, `raster`
and `render` draw them, `hud` annotates them. Entry point is
`../simulate.py`.
"""
